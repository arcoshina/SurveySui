# Pass 註銷機制 — 跟進清單

> 主設計見 [`專案 Pass停權.md`](./專案%20Pass停權.md)。  
> **儲存決策（本文件）**：已註銷 nullifier **清單**以 **BFF 託管在 Cloudflare 平台的資料庫為權威（主）**，**Walrus 為輔**（歸檔／備份／稽核）；**上線初期不做鏈上 `revoked_nullifiers` Table**。

---

## 一、確定不做的事

下列項目在本階段**明確排除**，勿為「做完整」而提前實作；日後僅在收入、體量與合規需求足以支應時，再另開評估。

### 鏈上與合約

| 項目 | 說明 |
|------|------|
| **鏈上 `revoked_nullifiers` Table** | 千筆級 storage 成本不可接受；填答止損不靠此表。 |
| **`REVOCATION_ON_CHAIN_SYNC=true`** | 管理員註銷後不追加「寫鏈上黑名單表」交易。 |
| **進階 package `admin_rescue_revoke_with_chain_registry`** | 依賴上項 Table，本階段不部署、不呼叫。 |
| **鏈上 `admin_unrevoke_nullifier`** | 誤判恢復以 BFF unrevoke + 用戶重新驗證／`update_pass_credential` 為主。 |
| **`mint_pass` 鏈上查已註銷 nullifier** | Mint 阻擋由 BFF 簽 Ticket 前查庫負責，不在合約預設路徑加 assert。 |
| **填答 `claim` 傳入 `NullifierRegistry` 查全域黑名單** | 填答資格靠 Pass 內 slot `REVOKED` + `is_source_valid`（OR 語意）。 |
| **Merkle Tree／非成員證明** | 複雜度與 Gas 過高，本階段不採用。 |
| **以刪除 `CredentialSlot` 代表註銷** | 僅標記 `CREDENTIAL_REVOKED`，保留 slot 與 UI 可讀性。 |
| **「Pass 內曾有任一來源被註銷 → 整本拒絕填答」** | 與細粒度止損、誤報保護衝突。 |

### BFF／營運

| 項目 | 說明 |
|------|------|
| **一般填答依賴 BFF 即時 Ticket** | `claim` 自付／代付皆不依 BFF；安全邊界在鏈上。 |
| **自願刪除 Pass 自動寫入已註銷庫** | 僅管理員救援註銷或 `--offline-only` 離線登記寫庫。 |
| **Walrus 作為 Mint 查詢的權威來源** | 查詢以 Cloudflare 上 BFF DB 為準；Walrus 僅輔助歸檔，不參與即時拒簽路徑（除非日後明確改架構）。 |

### 文件與設定

| 項目 | 說明 |
|------|------|
| **`.env` 預設開啟鏈上同步** | `REVOCATION_ON_CHAIN_SYNC` 維持 `false`。 |
| **實作 `REVOCATION_REGISTRY_BACKEND=on_chain` 類選項** | 本階段後端矩陣僅 **Cloudflare BFF DB（主）+ 可選 Walrus（輔）**。 |

---

## 二、現在要做的事

依 [`專案 Pass停權.md`](./專案%20Pass停權.md) §六，第 1～4 步鏈上與 BFF 骨架已存在；本階段收尾重點如下。

### 2.1 儲存與環境（主：Cloudflare BFF DB）

| # | 工作 | 說明 | 狀態 |
|---|------|------|------|
| 1 | **Production 註銷庫落在 Cloudflare** | BFF 部署於 Cloudflare 時，已註銷清單使用平台託管資料庫（建議 **D1** 或與現有 `@libsql/client` 相容的 **Turso／遠端 libSQL**），**不可**依賴單機 `data/surveysui.db` 檔案。 | 待做 |
| 2 | **本地開發與 Production 分離** | 開發可繼續本地 SQLite；Production 透過環境變數指向 Cloudflare 資料庫連線。 | 待做 |
| 3 | **`REVOCATION_REGISTRY_BACKEND` 語意收斂** | 上線設定為 **`bff_db`**（即 Cloudflare 權威庫）；含 Walrus 時為 **`bff_db+walrus`**（先寫 DB，再非同步歸檔 Walrus）。程式尚未讀取此變數，需補上或於文件／部署清單固定為上述值。 | 待做 |
| 4 | **`REVOCATION_MINT_GUARD_ENABLED` 與文件一致** | 文件預設 `true`；程式應**預設開啟**，僅 `false` 時關閉（避免 production 漏設導致 Mint 防線全關）。 | 待做 |
| 5 | **維持 `REVOCATION_ON_CHAIN_SYNC=false`** | CLI／BFF 不送鏈上寫表交易。 | 已符合決策 |

**寫入流程（目標）**

1. 鏈上 `admin_rescue_revoke` 成功 → slot `REVOKED`（**必做，權威止損**）。
2. CLI 或 BFF 管理 API → **寫入 Cloudflare BFF DB**（**主，Mint 查詢用**）。
3. 若啟用 Walrus 輔助 → **非同步**寫入 blob（失敗不阻擋主流程，需 log／重試）。

**Pass 已刪除**：僅步驟 2（與可選 3），`--offline-only`，不送鏈上 tx。

### 2.2 Walrus（輔）

| # | 工作 | 說明 | 狀態 |
|---|------|------|------|
| 6 | **Walrus 歸檔介面** | 在 BFF 或 `admin_rescue.ts` 於 DB 寫入成功後，可選將 `{ nullifier_hash, source, revoked_at, pass_id, reason }` 發布至 Walrus。 | 待做 |
| 7 | **`.env` Walrus 變數** | `REVOCATION_WALRUS_PUBLISHER_URL`、`REVOCATION_WALRUS_EPOCHS`；未設定則跳過 Walrus，不影響主庫。 | 範本已有 |
| 8 | **失敗策略** | Walrus 失敗：**不**回滾 Cloudflare DB；記錄告警，供事後補歸檔。 | 待訂 |

### 2.3 測試（優先）

| # | 工作 | 說明 | 狀態 |
|---|------|------|------|
| 9 | **Move：`do_delete` 回歸** | 舊／已註銷 Pass 刪除不得釋放新 Pass 的 `registry.used`；`registry.passes` 須比對 object ID。 | 待做 |
| 10 | **Move：細粒度註銷** | 同一 Pass 有 Google + World ID，僅註銷 Google 時，World ID 仍 `is_source_valid`／`check_eligibility` 通過。 | 待做 |
| 11 | **BFF：已註銷拒簽 Mint Ticket** | `isNullifierRevoked` 為 true 時 auth handler 拒絕。 | 待做 |
| 12 | **BFF：Mint 頻控** | 同 nullifier+source 在 `REVOCATION_MINT_TICKET_RATE_LIMIT_HOURS` 內不可重複簽票。 | 待做 |
| 13 | **整合：admin_rescue CLI** | 鏈上註銷 + BFF `/api/admin/revocation/revoke` 雙寫；`--offline-only` 僅 BFF。 | 待做 |

### 2.4 鏈上（已實作，僅維護）

下列已存在於 `survey_pass.move`，本階段**不需**再加鏈上註銷表；僅隨測試補強信心：

- `CredentialSlot.status`、`admin_rescue_revoke`
- `is_source_valid` / `check_eligibility` OR 語意
- `mint_pass` → `EPassAlreadyExists`
- `do_delete` 條件釋放 `used`
- `self_delete_sponsored_pass` 動態規費

### 2.5 可並行、非阻擋上線

| # | 工作 | 說明 |
|---|------|------|
| 14 | 前端：allowed_sources 與 ACTIVE 憑證無交集時提示 | UX，見主文件 §4.5 |
| 15 | Gas sponsor：代付 `claim` 前鏈上讀 Pass 預檢 | 省 sponsor gas，非安全必須 |
| 16 | 同步 `Pass停權設計討論紀錄.md` 與本跟進文件 | 文件債 |

---

## 三、上線前要確認的事

### 3.1 環境與部署

- [ ] Production BFF 已註銷庫為 **Cloudflare 託管資料庫**，非本機檔案路徑。
- [ ] `REVOCATION_MINT_GUARD_ENABLED=true`（或程式預設等效為開啟）。
- [ ] `REVOCATION_ON_CHAIN_SYNC=false`（或未設定且視同 false）。
- [ ] `REVOCATION_REGISTRY_BACKEND=bff_db` 或 `bff_db+walrus`（**勿**依賴未實作的 `walrus` 單獨後端作為唯一權威）。
- [ ] `REVOCATION_MINT_TICKET_RATE_LIMIT_HOURS` 已設（建議 `1`）。
- [ ] 管理員 API（`/api/admin/revocation/*`）僅內網或強認證可達；Secrets 已納入上線前輪換清單（見 `V4_Tasks.md`）。

### 3.2 鏈上行為（手動或自動化 E2E）

- [ ] `admin_rescue_revoke` 後，該 **source** slot 為 `REVOKED`，`registry.used` 已釋放該 nullifier。
- [ ] 問卷只接受被註銷來源 → **claim 失敗**（自付／代付皆然）。
- [ ] 問卷接受其他仍 **ACTIVE** 的來源 → **仍可 claim**（誤註銷保護）。
- [ ] 已註銷 nullifier 向 BFF 申請 Mint Ticket → **拒簽**。
- [ ] Pass 已刪除後，`admin_rescue.ts --offline-only` → Cloudflare DB 有紀錄，下次同身分 Mint 仍被拒。
- [ ] **未**部署鏈上 `revoked_nullifiers`；管理員工具**未**送出寫表交易。

### 3.3 Walrus（若已啟用輔助）

- [ ] DB 寫入成功後 Walrus 歸檔可選執行；Walrus 暫時不可用時，Mint 守衛仍僅依 Cloudflare DB。
- [ ] 歸檔內容可供事後稽核（nullifier、source、時間、備註），不要求填答或 Mint 即時讀 Walrus。

### 3.4 測試與文件

- [ ] Move 測試 §2.3 #9、#10 已通過。
- [ ] BFF 測試 §2.3 #11、#12 已通過（或同等整合測試）。
- [ ] `V4_Tasks.md`「撤銷 Pass 的機制」可拆項勾選：**核心止損（鏈上 slot + Cloudflare DB）** 與 **Walrus 輔助** 分開標記。
- [ ] 對外／前端說明：填答不依 BFF；管理員可細粒度註銷單一來源；自願刪除不等於永久封鎖身分。

### 3.5 已知取捨（確認團隊共識即可）

- 填答安全邊界在 **鏈上 slot 狀態**；BFF 庫主要擋 **重 Mint／加憑證**。
- 主動刪除 Pass **不**自動進已註銷庫；洗白窗口靠 **Mint 頻控 + 管理員 offline 登記** 緩解。
- 代付 `claim` 若未做鏈上預檢，失敗交易可能消耗 sponsor gas，但**不**構成安全繞過。

---

## 附：與主文件差異摘要

| 主文件 §4.1 泛稱 | 本跟進定案 |
|------------------|------------|
| `bff_db` | **Cloudflare 上 BFF 託管資料庫**（Production 權威） |
| `walrus` / `walrus+bff_db` | **`bff_db+walrus`**：DB 主、Walrus 輔 |
| `REVOCATION_ON_CHAIN_SYNC` 可選 true | **上線初期固定 false，不做** |

*最後更新：依產品決策「Cloudflare 主 + Walrus 輔、不做鏈上註銷表」整理。*
